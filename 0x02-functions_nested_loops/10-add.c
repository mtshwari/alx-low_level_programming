#include "main.h"

/**
 * add - adds two integers and returns the result
 * @p: int to be added to m
 * @m: int to be added to p
 * Return: sum of p and m
 */
int add(int p, int m)
{
	int n;

	n = p + m;

	return (n);
}
